datatype Tree = Node (left: Tree, val: int, right: Tree) | Leaf;

function rec Sum (t: Tree) returns (s: int)
con decreases t; noc
let
  s = match t with
  | Leaf : 0
  | Node (l, v, r) : Sum(l) + v + Sum(r)
  end;
tel

node main (t: Tree) returns ()
let
  check Sum(t) = Sum(t);
tel
