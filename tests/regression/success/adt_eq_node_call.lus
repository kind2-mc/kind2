
datatype D = C1 (val: int) | C2 (val: real);

node make(i: int) returns (d: D);
let
  d = C1(i);
tel

node main(i: int) returns (ok: bool);
var x, y: D;
let
  x = make(i);
  y = make(i);
  ok = x = y;
  check ok;
tel
