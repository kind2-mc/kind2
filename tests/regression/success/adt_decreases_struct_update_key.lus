datatype Nat = Succ (pred: Nat) | Zero;

function rec F (n: Nat) returns (out: int)
con decreases n; noc
let
  out = match n with
  | Zero : 0
  | Succ (p) : (map[]@<int, int>[F(p) := 1])[0]
  end;
tel

node main (n: Nat) returns ()
let
  check F(n) = F(n);
tel
