-- Wrapping x in Some and immediately pattern-matching extracts x.
datatype IntOpt = None | Some (int);

node main(x: int) returns (ok: bool);
var opt: IntOpt;
var out: int;
let
  opt = Some(x);
  out = match opt with
    | Some(v) : v
    | None : 0
  end;
  ok = out = x;
  --%PROPERTY ok;
tel
