type Point = { x: int; y: int };
datatype Shape = Pt (Point) | None;

node main(p1, p2: Point) returns (ok: bool);
var a, b: Shape;
let
  a = Pt(p1);
  b = Pt(p2);
  ok = (a = b) = (p1.x = p2.x and p1.y = p2.y);
  check ok;
tel
