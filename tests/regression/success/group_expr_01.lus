

node M(c:bool) returns (z:int);
let
  z = if c then 7 else 8;
tel

node N(c:bool) returns (y1,y2,y3,z1,z2:int);
let
  y1, y2, y3 = if c then (1,(2,3)) else (4,(5,6));
  z1, z2 = (M(c), M(not c));
tel
