datatype D = Wrap (val: [int, int]) | Empty;

node main(a, b, c, d: int) returns (ok: bool);
var x, y: D;
let
  x = Wrap('(a, b));
  y = Wrap('(c, d));
  ok = (x = y) = (a = c and b = d);
  check ok;
tel
