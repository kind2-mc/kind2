datatype L = Nil | Cons (hd: int, tl: L);

function rec len (l: L) returns (n: int);
(*@contract
  decreases l;
*)
let
  match l with
  | Nil:
    n = 0;
  | Cons (h, t):
    n = 1 + len(t);
  end
tel

node main () returns (ok: bool);
let
  ok = len(Cons(1, Cons(2, Nil))) = 2;
  check ok;
tel
