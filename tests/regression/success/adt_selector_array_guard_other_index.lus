datatype IntOpt = None | Some (val: int);

node main(a: IntOpt^5^2) returns (p: int^5^2; o: int^5^2);
let
  p[i][j] = 0;
  o[j][i] = 0 -> pre (when not None?(a[j][i]) and not None?(a[0][i]) then a[0][i].val else 0);
  check o[0][0] = o[0][0];
tel
