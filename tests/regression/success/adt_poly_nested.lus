datatype Opt<T> = Absent | Present (T);

node main(i: int) returns (ok: bool);
var inner: Opt<int>;
var outer: Opt<Opt<int>>;
let
  inner = Present(i);
  outer = Present(inner);
  ok = outer = Present(Present(i));
  check ok;
tel
