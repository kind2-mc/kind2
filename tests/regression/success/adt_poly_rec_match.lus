datatype List<T> = Nil | Cons (hd: T, tl: List<T>);

node main(i: int) returns (ok: bool);
var x: List<int>;
var h: int;
let
  x = Cons(i, Nil@<int>);
  h = match x with
    | Nil : 0
    | Cons(a, r) : a
  end;
  ok = h = i;
  check ok;
tel
