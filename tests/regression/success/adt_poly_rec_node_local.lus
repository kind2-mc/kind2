datatype List<T> = Nil | Cons (hd: T, tl: List<T>);

node depth<A>(x: A) returns (n: int);
var l: List<A>;
let
  l = Cons(x, Nil@<A>);
  n = match l with
    | Nil : 0
    | Cons(a, r) : 1
  end;
tel

node main(i: int) returns (ok: bool);
let
  ok = depth@<int>(i) = 1;
  check ok;
tel
