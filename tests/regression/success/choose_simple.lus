const x1: int = 0;
type Nat = subtype { x: int | x >= 0 };

node main (a: int) returns (y, z: int); 
var x2, b: Nat;
let 
  x2 = choose { x: Nat | x > x1 };
  y = choose { x: int | x + x2 < 50 };
  check y < 100;
  
  b = a + 10;
  z = choose { x: int | a <= x and x <= b assuming a<=b };
  check a <= z and z <= b;
tel;
