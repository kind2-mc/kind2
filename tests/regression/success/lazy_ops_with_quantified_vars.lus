

type Pos = subrange [1,*] of int;

const N: Pos;

transparent function AddOne(i: Pos) returns (r: Pos);
let
  r = i + 1;
tel

node N() returns (B: int^N);
let
   B[i] = if i=0 then 0 otherwise AddOne(i);
   check "P1" exists (i: Pos) i+1=AddOne(i);
tel