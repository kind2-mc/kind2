function F(x:int) returns (y:int);
(*@contract
  assume "A" x >=0;
  guarantee "G" y>0;
*)
let
  y = x + 1;
tel

node N(x:int) returns (z: int)
let
  z = when x>=0 then F(x) else 1;
  check "P" z>0;
tel 