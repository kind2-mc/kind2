datatype List<T> = Nil | Cons (hd: T, tl: List<T>);

node main(l: List<int>) returns (ok: bool);
var h: int;
let
  match l with
  | Nil:
    h = 0;
  | Cons (a, r):
    h = a;
  end
  ok = h > 0;
  check ok;
tel
