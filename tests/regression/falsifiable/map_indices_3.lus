type M = struct {
  f1: int;
  f2: bool;
};

type Set = map<M,bool>;

const N: subtype {x : int | x > 3}; 
const M: subtype {x : int | x > 3}; 

node N(m: M) returns (A: int^N^M; s: Set);
let
  A = any@<int^N^M>;
  s = map[]@<M,bool>;
  check m in s;
tel
