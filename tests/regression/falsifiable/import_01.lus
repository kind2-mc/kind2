contract bla_spec(i: real) returns (out: bool) ;
let
  guarantee out = (i > 0.0);
tel

node bli(i: real) returns (out: bool) ;
(*@contract
  import bla_spec (i) returns (out) ;
*)
let
  out = i >= 0.0 ;
tel