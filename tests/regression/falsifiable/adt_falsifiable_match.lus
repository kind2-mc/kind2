-- Not all constructors produce true, so this check is falsifiable.
datatype Color = Red | Green | Blue;

node main(c: Color) returns (ok: bool);
let
  ok = match c with
    | Red : true
    | Green : false
    | Blue : true
  end;
  check ok;
tel
