transparent function F(inp: int) returns (out: int)
con
  assume inp > 0;
  guarantee out < 0;
noc
let
  out = inp + 1;
tel

function G(inp: int) returns (out: int);
let
  out = F(inp) + 1;
tel

node N() returns ()
let
  check forall (i: int) G(i) < i;
tel
