datatype Opt<T> = Absent | Present (val: T);

node N(i: int; r: real) returns (vi: int; vr: real);
var xi: Opt<int>;
var xr: Opt<real>;
let
  xi = Present(i);
  xr = Present(r);
  vi = xi.val;
  vr = xr.val;
tel
