-- Illegal call to a stateful contract in a function.
contract blah_spec (i: int) returns (out: int) ;
let
  guarantee out = (0 -> pre i) ;
tel

function blah ( i: int ) returns ( out: int ) ;
(*@contract
  import blah_spec (i) returns (out) ;
*)
let
  out = i ;
tel
