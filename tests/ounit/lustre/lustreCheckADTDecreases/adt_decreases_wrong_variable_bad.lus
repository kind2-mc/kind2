datatype Nat = Succ (pred: Nat) | Zero;

function rec F (n: Nat; k: Nat) returns (out: int)
con decreases n; noc
let
  out = match n with
  | Zero : 0
  | Succ (m) : (match k with
                | Zero : 1 + F(n, k)
                | Succ (j) : 1 + F(n, j)
                end)
  end;
tel
