datatype Nat = Succ (pred: Nat) | Zero;

function split (n: Nat) returns (a: Nat; b: Nat);
let
  a = n;
  b = n;
tel

function rec F (n: Nat; m: Nat) returns (out: int)
con decreases n; noc
let
  out = match n with
  | Zero : 0
  | Succ (p) : 1 + F(split(p))
  end;
tel
