datatype List = Cons (head: int, tail: List) | Nil;

function rec F (n: List) returns (out: int)
con decreases n; noc
var y : subtype { n : List | (match n with | Nil : true | Cons (h, tl) : F(tl) >= 0 end) };
let
  y = Nil;
  out = match n with
  | Nil : 0
  | Cons (h, tl) : 1 + F(tl)
  end;
tel
