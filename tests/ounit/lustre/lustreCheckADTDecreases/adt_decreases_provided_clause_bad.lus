datatype Nat = Succ (pred: Nat) | Zero;

function rec F (n: Nat) returns (out: bool)
con decreases n; noc
let
  out = match n with
  | Zero : true
  | Succ (m) : F(m)
  end;
  check "buggy" out provided F(n);
tel
