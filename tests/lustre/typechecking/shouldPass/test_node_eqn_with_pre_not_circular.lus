node a (x: int) returns (y: int; z: int);
var p:int;
var q:int;
let
   q = pre p;
   p = q;
   y = pre z;
   z = pre y;
tel

node b (x: int) returns (y: int);
var p: int;
var q: int;
let
   q = pre p;
   p = pre q;
tel
