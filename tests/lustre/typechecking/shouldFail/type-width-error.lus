const n = 11;
const w = 5;
const t = true;

node f(x : bool) returns (b1 : bool^n);
var k:bool ^ n;
let
  k = false ^ n;
tel

node b(x : bool) returns (xb : bool^w);
let
  --%PROPERTY xb[1] = false;
  xb = f(x);
tel