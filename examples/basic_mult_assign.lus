node test(x: int) returns (y1, y2, y3: int; p1, p2, p3: bool);
let 
  p1 = (y1 = 240);
  p2 = (y2 = 34);
  --p3 = (y3 = 10);
  --%PROPERTY p1;
  --%PROPERTY p2;
  (y1, y2) = node3(node2(2+2, 3+3));
  y3 = if x > 29 then 36 else 12 -> 43 + if x > 34 then 43 else 23;
tel

node node2(a, b: int) returns (c, d: int);
let
   c = a + b;
   d = a * b;
tel

node node3(a, b: int) returns (c, d: int);
let
   c = a * b;
   d = a + b;
tel

node node1(a: int ) returns (c: int);
let
   c = a + 2;
tel

