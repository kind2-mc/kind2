node main() returns (P1: bool);
var out1: int;
var out2: int;
let
  out1, out2 = test(-1);
  P1 = (out1 + out2 >= 0);

  check P1;
  --%MAIN;
tel

node test (init : int) returns (out1, out2 : int);
let
    out1 = 0 -> pre out1 + 1;
    out2 = init -> pre out2 + 1;
tel
(*
node counter2 (init : int) returns (out : int);
let
    out = init -> pre out + 1;
tel
*)