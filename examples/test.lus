node test(x: int) returns (y1: int^3; p1: bool);
var helper: int^3;
let 
  p1 = (y1[0] = 1);
  --%PROPERTY p1;
  
  helper = [1, 4, -3];
  y1 = if x > 1 then [1, 4, -3] else helper;
  --y1 = helper;
tel
